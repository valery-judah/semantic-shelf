# Books Recommendation API (MVP)

## Requirements
- Python 3.11+
- uv

## Commands
- `make sync` - install dependencies with uv
- `make run` - run FastAPI app at http://localhost:8000
- `make test` - run tests
- `make fmt` - format and auto-fix lint
- `make lint` - lint check
- `make type` - run mypy
